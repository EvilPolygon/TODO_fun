const db = require('../db')

class UserController {

    async createUser(req, res) {
        /* const {name, lastname} = req.body
        const newUser = await db.query(`INSERT INTO users (name, lastname) values ($1, $2) returning *`, [name, lastname])

        res.json(newUser.rows) */
    }

}